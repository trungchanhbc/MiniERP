<Window x:Class="MiniERP.View.RawMaterialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniERP.View"
        mc:Ignorable="d"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        
        DataContext="{StaticResource RawMaterialVM}"
        
        WindowStartupLocation="CenterScreen"
        
        Title="{StaticResource RawMaterial_Title}" Height="650" Width="1550">
    <Grid>
        <Grid.Resources>
            <Style TargetType="materialDesign:Card">
                <Setter Property="Margin" Value="10" />
            </Style>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar-->
        <materialDesign:Card Grid.Row="0" Margin="10,10,10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                
                <ComboBox x:Name="cboRMSearch" Margin="5" Grid.Column="0"
                          materialDesign:HintAssist.Hint="{StaticResource RawMaterial_SearchBy}"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                    <ComboBoxItem>
                        Input Date
                    </ComboBoxItem>
                    <ComboBoxItem>
                        Customs Declaration No.
                    </ComboBoxItem>
                    <ComboBoxItem>
                        Open Date
                    </ComboBoxItem>
                    <ComboBoxItem>
                        Goods Arrival Date
                    </ComboBoxItem>
                    <ComboBoxItem>
                        ...
                    </ComboBoxItem>
                </ComboBox>

                <TextBox x:Name="txbRMSearch" Margin="5" Grid.Column="1"
                         materialDesign:HintAssist.Hint="{StaticResource RawMaterial_SearchText}"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <StackPanel Orientation="Horizontal" Grid.Column="2">
                    <Button Command="{Binding OpenRawMaterialInputCommand}"
                            Margin="5" Width="100"
                            Style="{StaticResource MaterialDesignRaisedButton}" Content="{StaticResource Add}"/>
                    <Button Margin="5" Width="100"
                            Style="{StaticResource MaterialDesignRaisedButton}" Content="{StaticResource Edit}"/>
                    <Button Margin="5" Width="100"
                            Style="{StaticResource MaterialDesignRaisedButton}" Content="{StaticResource Remove}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
        
        <!-- Raw Material DataGrid -->
        <materialDesign:Card Grid.Row="1">
            <ListView>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{StaticResource RawMaterial_InputDate}" />
                        <GridViewColumn Header="{StaticResource RawMaterial_CustomsDeclarationNo.}" />
                        <GridViewColumn Header="{StaticResource RawMaterial_OpenDate}" />
                        <GridViewColumn Header="{StaticResource RawMaterial_GoodsArrialDate}" />
                        <GridViewColumn Header="{StaticResource RawMaterial_Code}" />
                        <GridViewColumn Header="{StaticResource RawMaterial_Name}" />
                        <GridViewColumn Header="{StaticResource RawMaterial_HSCode}" />
                        <GridViewColumn Header="{StaticResource Price}" />
                        <GridViewColumn Header="{StaticResource ExchangeRate}" />
                        <GridViewColumn Header="{StaticResource TaxRate}" />
                        <GridViewColumn Header="{StaticResource Tax}" />
                        <GridViewColumn Header="{StaticResource ExportPrice}" />
                        <GridViewColumn Header="{StaticResource Input}" />
                        <GridViewColumn Header="{StaticResource Output}" />
                        <GridViewColumn Header="{StaticResource Stock}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </materialDesign:Card>
    </Grid>
</Window>
